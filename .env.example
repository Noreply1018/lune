# Lune 生产 compose 环境变量示例。复制为 .env 后按需修改。
# docker compose -f docker-compose.prod.yml --env-file .env up -d

# --- 镜像版本 ---
# 默认从 GHCR 拉取；如需 Docker Hub，可改为 docker.io/noreply1018/lune
LUNE_IMAGE=ghcr.io/noreply1018/lune

# 默认 latest；锁版本建议填 0.1.3 之类的 tag（不带 v 前缀）
LUNE_IMAGE_TAG=latest

# --- Lune ---
# HTTP 端口（容器内 + 宿主机）
LUNE_PORT=7788

# Admin Token；本机使用可留空，由 Lune 首次启动自动生成。
# 远程访问、反代部署或不可信网络中请显式设置。
LUNE_ADMIN_TOKEN=

# 网关大请求限制可在 Settings 页面调整；默认 100MB，上方无需写入 .env

# --- 日志（可选）---
# LUNE_LOG_LEVEL=info       # debug | info | warn | error
# LUNE_LOG_FORMAT=text      # text | json
