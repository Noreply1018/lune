# CPA (CLI Proxy API) config for Lune docker-compose setup.
# Mounted into the container at /CLIProxyAPI/config.yaml.

port: 8317

auth-dir: "/app/cpa-auth"

remote-management:
  allow-remote: true
  secret-key: "lune-cpa-management-dev"

api-keys:
  - "sk-cpa-default"

debug: false
